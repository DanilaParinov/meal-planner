# Database
DB_HOST=localhost
DB_PORT=5433
DB_USER=changeme_user
DB_PASSWORD=changeme_password
DB_NAME=meal_planner
DB_SSLMODE=disable

# Server
SERVER_PORT=8080
SERVER_ENV=development

# Test users are seeded by migrations/002_seed_test_data.sql for local dev.
# Real users and admins are created with: go run cmd/createuser/main.go -name "..." [-admin]
